#!/bin/bash

function br-note () {
    # 1. generate config
    #    use x86_64
    #    use ext4 fs
    #    install package like openssh tcpdump iptables etc..
    #    you could use preconfiged config file like ./.config.mini
    # 2. unset LD_LIBRARY_PATH
    # 3. make -j10
    return
}
